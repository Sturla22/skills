# Work Plan

## Problem statement

Clarify commit-request handling in the repo's operating model without widening the workflow surface unnecessarily.

## Approach

1. Update the shared operating-model wording in `AGENTS.md`.
2. Mirror the clarification in `docs/operating-model.md`.
3. Record the workflow experiment and changelog impact.
4. Verify the wording stays narrow: it should authorize logical commits, not silent history rewriting or unrelated commit splitting.
